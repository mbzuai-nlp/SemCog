# Core dependencies
requests>=2.28.0
aiohttp>=3.8.0
tqdm>=4.64.0
python-dotenv>=1.0.0

# Data analysis
pandas>=2.0.0
numpy>=1.24.0

# LLM evaluation (optional - only needed for open-source model evaluation)
# lm-eval>=0.4.0

# API clients (optional - only needed for specific API models)
# openai>=1.0.0
